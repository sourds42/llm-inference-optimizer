# GPU-only deps, installed inside notebooks/run_on_colab.ipynb's setup cell.
# Deliberately lightweight -- no vllm/llmcompressor/lm-eval -- so setup
# takes a couple of minutes instead of ten-plus. See src/quantize.py and
# src/benchmark.py for what that trades away (GPTQ/AWQ, vLLM serving
# metrics, lm_eval accuracy) in exchange for speed.
transformers>=4.44
accelerate
bitsandbytes
datasets
huggingface_hub
